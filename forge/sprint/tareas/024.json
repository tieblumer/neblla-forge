{
  "id": "024",
  "num": 24,
  "title": "Construir el \"ciclo de reconstrucción\" del forge: un orquestador que abre una rama nueva (todo el worktree pasa a la rama, no a master, para tener un master-vs-rama con qué comparar), hace que Anselmo documente todos los cambios en backbone + MCP, lanza 4 veces en paralelo al mismo apóstol con ángulos distintos (Lucas=tests, Marcos=definición, Juan=diff, Mateo=bordes/huecos) para verificar que la documentación está completa, deja que Lina+Ana Liz redacten plan y tests mirando solo tests+worktree de Anselmo, corre la batería nueva contra MASTER como \"gate de oro\", limpia de la rama todo el código que no sean los tests, vuelca plan+docs (sin los libros de los apóstoles), y suelta un Miguel gigante en ultracode que reprograma desde el código de master hasta que todos los tests pasan, con reanudación si se cae.",
  "body": "Complejidad: compleja\n\nPartes:\n• orquestador — El driver determinista del ciclo, fence no cartel (al estilo de scripts/sprint.js + scripts/heart.js). Secuencia los pasos en orden fijo y posee ramas, worktrees, gate y caps. Pasos: (1) crear rama de ciclo y mover el worktree a ella (no a master) → guarda baseRef=master para los diffs; (2) lanzar a Anselmo a documentar TODO en backbone + reflejarlo en el MCP, sobre su propio worktree; (3) lanzar 4 veces EN PARALELO al apóstol —cada instancia arranca de cero, recibe el worktree de Anselmo + su ángulo (Lucas/Marcos/Juan/Mateo) + baseRef para el diff master-vs-rama— y devuelve su testimonio verificando que Anselmo describió bien todas las features; (4) Lina+Ana Liz redactan plan y tests mirando SOLO los tests + el worktree de Anselmo (el plan y los tests viven en ese worktree), con la lista de Mateo como entrada OBLIGATORIA que se convierte en tests nuevos; (5) GATE DE ORO: correr la batería nueva contra el código de MASTER —verde = los tests describen lo que ya existía, no inventan; rojo aborta el ciclo; (6) borrar de la rama todo el código que no sean los tests; (7) volcar la rama con el plan de Lina y la documentación de Anselmo, SIN los libros de los 4 apóstoles; (8) lanzar un Miguel gigante con esfuerzo ultracode en su propio worktree, que recibe la rama (tests+plan+docs) pero el código de master, y reprograma hasta que TODOS los tests pasan; (9) si Miguel se cae, arrancar otro que sigue desde donde lo dejó, en la misma rama (reanudación). Reutiliza worktrees.js, forge-merge.js, forge-shutdown.js, forge-tests.js/run-forge.js para correr la batería.  (ficheros: scripts/reconstruir.js, scripts/lib/forge-recon.js, scripts/lib/forge-firme.js, scripts/forge.js, scripts/sandbox/worktrees.js, scripts/lib/forge-merge.js)\n• voces — Los prompt-builders de cada rol del ciclo, sin estado de runtime (solo construyen el texto de arranque a partir de los datos que les pasa el orquestador). (a) anselmoDocPrompt: instruye a Anselmo a leer el diff master-vs-rama y documentar TODAS las features a fondo en el backbone y reflejar los cambios de feature en el MCP; (b) apostolPrompt: una sola plantilla parametrizada por ángulo que produce las 4 lentes —Lucas mira los tests (qué se garantiza), Marcos la definición de la tarea (el porqué), Juan el diff del commit (qué cambió fichero a fichero), Mateo los bordes y huecos (comportamiento real sin test)— recibe el worktree de Anselmo + baseRef y devuelve un testimonio que verifica la documentación; (c) linaReconPrompt: planifica la implementación completa mirando solo tests + worktree de Anselmo, integrando la lista de Mateo como tests obligatorios; (d) miguelGigantePrompt: reconstrucción ultracode desde código de master con tests+plan+docs como brújula, con variante de reanudación 'sigue desde donde lo dejó'.  (ficheros: scripts/lib/forge-prompts.js)\n\nContrato:\n• orquestador ↔ voces: Firmas fijas de los prompt-builders exportados por scripts/lib/forge-prompts.js, que el orquestador importa y llama. — anselmoDocPrompt({worktreeDir, baseRef, objetivo}) → string. apostolPrompt({angulo:'lucas'|'marcos'|'juan'|'mateo', worktreeDir, baseRef}) → string (una sola función, 4 ángulos). linaReconPrompt({testsDir, docsDir, huecosMateo}) → string. miguelGigantePrompt({rama, planPath, docsDir, masterRef, reanudacion:boolean}) → string. Todas devuelven SOLO el texto de arranque; ningún builder lee disco ni guarda estado. El orquestador es el único dueño de ramas/worktrees/gate y de pasar baseRef=master.",
  "fromChat": "050",
  "fromMsg": 8,
  "createdAt": "2026-06-07T19:56:26.496Z",
  "plan": {
    "version": 1,
    "resumen": "Construir el \"ciclo de reconstrucción\" del forge: un orquestador que abre una rama nueva (todo el worktree pasa a la rama, no a master, para tener un master-vs-rama con qué comparar), hace que Anselmo documente todos los cambios en backbone + MCP, lanza 4 veces en paralelo al mismo apóstol con ángulos distintos (Lucas=tests, Marcos=definición, Juan=diff, Mateo=bordes/huecos) para verificar que la documentación está completa, deja que Lina+Ana Liz redacten plan y tests mirando solo tests+worktree de Anselmo, corre la batería nueva contra MASTER como \"gate de oro\", limpia de la rama todo el código que no sean los tests, vuelca plan+docs (sin los libros de los apóstoles), y suelta un Miguel gigante en ultracode que reprograma desde el código de master hasta que todos los tests pasan, con reanudación si se cae.",
    "complejidad": "compleja",
    "partes": [
      {
        "name": "orquestador",
        "hace": "El driver determinista del ciclo, fence no cartel (al estilo de scripts/sprint.js + scripts/heart.js). Secuencia los pasos en orden fijo y posee ramas, worktrees, gate y caps. Pasos: (1) crear rama de ciclo y mover el worktree a ella (no a master) → guarda baseRef=master para los diffs; (2) lanzar a Anselmo a documentar TODO en backbone + reflejarlo en el MCP, sobre su propio worktree; (3) lanzar 4 veces EN PARALELO al apóstol —cada instancia arranca de cero, recibe el worktree de Anselmo + su ángulo (Lucas/Marcos/Juan/Mateo) + baseRef para el diff master-vs-rama— y devuelve su testimonio verificando que Anselmo describió bien todas las features; (4) Lina+Ana Liz redactan plan y tests mirando SOLO los tests + el worktree de Anselmo (el plan y los tests viven en ese worktree), con la lista de Mateo como entrada OBLIGATORIA que se convierte en tests nuevos; (5) GATE DE ORO: correr la batería nueva contra el código de MASTER —verde = los tests describen lo que ya existía, no inventan; rojo aborta el ciclo; (6) borrar de la rama todo el código que no sean los tests; (7) volcar la rama con el plan de Lina y la documentación de Anselmo, SIN los libros de los 4 apóstoles; (8) lanzar un Miguel gigante con esfuerzo ultracode en su propio worktree, que recibe la rama (tests+plan+docs) pero el código de master, y reprograma hasta que TODOS los tests pasan; (9) si Miguel se cae, arrancar otro que sigue desde donde lo dejó, en la misma rama (reanudación). Reutiliza worktrees.js, forge-merge.js, forge-shutdown.js, forge-tests.js/run-forge.js para correr la batería.",
        "ficheros": [
          "scripts/reconstruir.js",
          "scripts/lib/forge-recon.js",
          "scripts/lib/forge-firme.js",
          "scripts/forge.js",
          "scripts/sandbox/worktrees.js",
          "scripts/lib/forge-merge.js"
        ]
      },
      {
        "name": "voces",
        "hace": "Los prompt-builders de cada rol del ciclo, sin estado de runtime (solo construyen el texto de arranque a partir de los datos que les pasa el orquestador). (a) anselmoDocPrompt: instruye a Anselmo a leer el diff master-vs-rama y documentar TODAS las features a fondo en el backbone y reflejar los cambios de feature en el MCP; (b) apostolPrompt: una sola plantilla parametrizada por ángulo que produce las 4 lentes —Lucas mira los tests (qué se garantiza), Marcos la definición de la tarea (el porqué), Juan el diff del commit (qué cambió fichero a fichero), Mateo los bordes y huecos (comportamiento real sin test)— recibe el worktree de Anselmo + baseRef y devuelve un testimonio que verifica la documentación; (c) linaReconPrompt: planifica la implementación completa mirando solo tests + worktree de Anselmo, integrando la lista de Mateo como tests obligatorios; (d) miguelGigantePrompt: reconstrucción ultracode desde código de master con tests+plan+docs como brújula, con variante de reanudación 'sigue desde donde lo dejó'.",
        "ficheros": [
          "scripts/lib/forge-prompts.js"
        ]
      }
    ],
    "contrato": [
      {
        "entre": [
          "orquestador",
          "voces"
        ],
        "interfaz": "Firmas fijas de los prompt-builders exportados por scripts/lib/forge-prompts.js, que el orquestador importa y llama.",
        "acuerdo": "anselmoDocPrompt({worktreeDir, baseRef, objetivo}) → string. apostolPrompt({angulo:'lucas'|'marcos'|'juan'|'mateo', worktreeDir, baseRef}) → string (una sola función, 4 ángulos). linaReconPrompt({testsDir, docsDir, huecosMateo}) → string. miguelGigantePrompt({rama, planPath, docsDir, masterRef, reanudacion:boolean}) → string. Todas devuelven SOLO el texto de arranque; ningún builder lee disco ni guarda estado. El orquestador es el único dueño de ramas/worktrees/gate y de pasar baseRef=master."
      }
    ],
    "aprobado": true,
    "aprobadoAt": "2026-06-08T04:35:08.641Z"
  },
  "subtareas": [
    {
      "name": "orquestador",
      "alcance": {
        "archivos": [
          "scripts/reconstruir.js",
          "scripts/lib/forge-recon.js",
          "scripts/lib/forge-firme.js",
          "scripts/forge.js",
          "scripts/sandbox/worktrees.js",
          "scripts/lib/forge-merge.js"
        ],
        "frontera": "El driver determinista del ciclo, fence no cartel (al estilo de scripts/sprint.js + scripts/heart.js). Secuencia los pasos en orden fijo y posee ramas, worktrees, gate y caps. Pasos: (1) crear rama de ciclo y mover el worktree a ella (no a master) → guarda baseRef=master para los diffs; (2) lanzar a Anselmo a documentar TODO en backbone + reflejarlo en el MCP, sobre su propio worktree; (3) lanzar 4 veces EN PARALELO al apóstol —cada instancia arranca de cero, recibe el worktree de Anselmo + su ángulo (Lucas/Marcos/Juan/Mateo) + baseRef para el diff master-vs-rama— y devuelve su testimonio verificando que Anselmo describió bien todas las features; (4) Lina+Ana Liz redactan plan y tests mirando SOLO los tests + el worktree de Anselmo (el plan y los tests viven en ese worktree), con la lista de Mateo como entrada OBLIGATORIA que se convierte en tests nuevos; (5) GATE DE ORO: correr la batería nueva contra el código de MASTER —verde = los tests describen lo que ya existía, no inventan; rojo aborta el ciclo; (6) borrar de la rama todo el código que no sean los tests; (7) volcar la rama con el plan de Lina y la documentación de Anselmo, SIN los libros de los 4 apóstoles; (8) lanzar un Miguel gigante con esfuerzo ultracode en su propio worktree, que recibe la rama (tests+plan+docs) pero el código de master, y reprograma hasta que TODOS los tests pasan; (9) si Miguel se cae, arrancar otro que sigue desde donde lo dejó, en la misma rama (reanudación). Reutiliza worktrees.js, forge-merge.js, forge-shutdown.js, forge-tests.js/run-forge.js para correr la batería.",
        "noTocar": [
          "scripts/lib/forge-prompts.js"
        ]
      },
      "contrato": [
        {
          "entre": [
            "orquestador",
            "voces"
          ],
          "interfaz": "Firmas fijas de los prompt-builders exportados por scripts/lib/forge-prompts.js, que el orquestador importa y llama.",
          "acuerdo": "anselmoDocPrompt({worktreeDir, baseRef, objetivo}) → string. apostolPrompt({angulo:'lucas'|'marcos'|'juan'|'mateo', worktreeDir, baseRef}) → string (una sola función, 4 ángulos). linaReconPrompt({testsDir, docsDir, huecosMateo}) → string. miguelGigantePrompt({rama, planPath, docsDir, masterRef, reanudacion:boolean}) → string. Todas devuelven SOLO el texto de arranque; ningún builder lee disco ni guarda estado. El orquestador es el único dueño de ramas/worktrees/gate y de pasar baseRef=master."
        }
      ]
    },
    {
      "name": "voces",
      "alcance": {
        "archivos": [
          "scripts/lib/forge-prompts.js"
        ],
        "frontera": "Los prompt-builders de cada rol del ciclo, sin estado de runtime (solo construyen el texto de arranque a partir de los datos que les pasa el orquestador). (a) anselmoDocPrompt: instruye a Anselmo a leer el diff master-vs-rama y documentar TODAS las features a fondo en el backbone y reflejar los cambios de feature en el MCP; (b) apostolPrompt: una sola plantilla parametrizada por ángulo que produce las 4 lentes —Lucas mira los tests (qué se garantiza), Marcos la definición de la tarea (el porqué), Juan el diff del commit (qué cambió fichero a fichero), Mateo los bordes y huecos (comportamiento real sin test)— recibe el worktree de Anselmo + baseRef y devuelve un testimonio que verifica la documentación; (c) linaReconPrompt: planifica la implementación completa mirando solo tests + worktree de Anselmo, integrando la lista de Mateo como tests obligatorios; (d) miguelGigantePrompt: reconstrucción ultracode desde código de master con tests+plan+docs como brújula, con variante de reanudación 'sigue desde donde lo dejó'.",
        "noTocar": [
          "scripts/reconstruir.js",
          "scripts/lib/forge-recon.js",
          "scripts/lib/forge-firme.js",
          "scripts/forge.js",
          "scripts/sandbox/worktrees.js",
          "scripts/lib/forge-merge.js"
        ]
      },
      "contrato": [
        {
          "entre": [
            "orquestador",
            "voces"
          ],
          "interfaz": "Firmas fijas de los prompt-builders exportados por scripts/lib/forge-prompts.js, que el orquestador importa y llama.",
          "acuerdo": "anselmoDocPrompt({worktreeDir, baseRef, objetivo}) → string. apostolPrompt({angulo:'lucas'|'marcos'|'juan'|'mateo', worktreeDir, baseRef}) → string (una sola función, 4 ángulos). linaReconPrompt({testsDir, docsDir, huecosMateo}) → string. miguelGigantePrompt({rama, planPath, docsDir, masterRef, reanudacion:boolean}) → string. Todas devuelven SOLO el texto de arranque; ningún builder lee disco ni guarda estado. El orquestador es el único dueño de ramas/worktrees/gate y de pasar baseRef=master."
        }
      ]
    }
  ],
  "paralelizadoAt": "2026-06-07T19:56:26.502Z",
  "threadId": "053",
  "testsPlan": {
    "version": 1,
    "tests": [
      {
        "ref": "voces",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "anselmoDocPrompt arma la instrucción de documentar a fondo el diff",
        "dado": "anselmoDocPrompt({worktreeDir, baseRef, objetivo}) con baseRef='master' y un worktreeDir concreto",
        "cuando": "se llama el builder",
        "entonces": "devuelve un string que (a) instruye leer el diff master-vs-rama usando baseRef, (b) pide documentar TODAS las features en el backbone y reflejar los cambios en el MCP, y (c) incrusta worktreeDir, baseRef y objetivo en el texto",
        "id": "T-24-01",
        "codigo": "const { anselmoDocPrompt } = await import(\"../../scripts/lib/forge-prompts.js\");\nr.ok(\"anselmoDocPrompt es una función\", typeof anselmoDocPrompt === \"function\");\nconst wt = \"/wt/anselmo-7f3a\";\nconst base = \"master\";\nconst obj = \"el forge\";\nconst s = anselmoDocPrompt({ worktreeDir: wt, baseRef: base, objetivo: obj });\nr.ok(\"devuelve un string no vacío\", typeof s === \"string\" && s.length > 0);\nr.ok(\"(a) manda leer el diff master-vs-rama usando baseRef\", /diff/i.test(s) && /(master|rama|baseRef)/i.test(s));\nr.ok(\"(b) manda documentar TODAS las features en el backbone\", /backbone/i.test(s) && /features?/i.test(s) && /(todas|todos|cada|a fondo)/i.test(s));\nr.ok(\"(b) manda reflejar los cambios en el MCP\", /\\bmcp\\b/i.test(s));\nr.ok(\"(c) incrusta el worktreeDir recibido\", s.includes(wt));\nr.ok(\"(c) incrusta el baseRef recibido\", s.includes(base));\nr.ok(\"(c) incrusta el objetivo recibido\", s.includes(obj));"
      },
      {
        "ref": "voces",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "apostolPrompt es una sola función con 4 ángulos distintos",
        "dado": "apostolPrompt({angulo, worktreeDir, baseRef}) llamada con angulo 'lucas', 'marcos', 'juan' y 'mateo'",
        "cuando": "se generan los cuatro prompts con los mismos worktreeDir/baseRef",
        "entonces": "se exporta UNA sola función y los cuatro strings son distintos entre sí, y cada uno refleja su lente (lucas=los tests/qué se garantiza, marcos=la definición/el porqué, juan=el diff fichero a fichero, mateo=bordes y huecos sin test)",
        "id": "T-24-02",
        "codigo": "const mod = await import(\"../../scripts/lib/forge-prompts.js\");\nr.ok(\"se exporta UNA sola función apostolPrompt\", typeof mod.apostolPrompt === \"function\");\nconst wt = \"/wt/anselmo-7f3a\";\nconst base = \"master\";\nconst lucas = mod.apostolPrompt({ angulo: \"lucas\", worktreeDir: wt, baseRef: base });\nconst marcos = mod.apostolPrompt({ angulo: \"marcos\", worktreeDir: wt, baseRef: base });\nconst juan = mod.apostolPrompt({ angulo: \"juan\", worktreeDir: wt, baseRef: base });\nconst mateo = mod.apostolPrompt({ angulo: \"mateo\", worktreeDir: wt, baseRef: base });\nconst arr = [lucas, marcos, juan, mateo];\nr.ok(\"los cuatro son strings no vacíos\", arr.every((x) => typeof x === \"string\" && x.length > 0));\nr.eq(\"los cuatro ángulos producen textos DISTINTOS entre sí\", new Set(arr).size, 4);\nr.ok(\"lucas = los tests / qué se garantiza\", /tests?/i.test(lucas) && /(garantiz|asegura|qu[eé] se)/i.test(lucas));\nr.ok(\"marcos = la definición / el porqué\", /(definici|porqu|tarea)/i.test(marcos));\nr.ok(\"juan = el diff fichero a fichero\", /diff/i.test(juan) && /fichero/i.test(juan));\nr.ok(\"mateo = bordes y huecos sin test\", /(borde|hueco)/i.test(mateo) && /test/i.test(mateo));"
      },
      {
        "ref": "voces",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "cada ángulo del apóstol recibe el worktree de Anselmo y baseRef para el diff",
        "dado": "apostolPrompt para cualquiera de los cuatro ángulos",
        "cuando": "se genera el prompt con worktreeDir (el de Anselmo) y baseRef='master'",
        "entonces": "el string incrusta el worktreeDir de Anselmo y baseRef, y pide verificar que la documentación describe bien todas las features",
        "id": "T-24-03",
        "codigo": "const { apostolPrompt } = await import(\"../../scripts/lib/forge-prompts.js\");\nconst wt = \"/wt/anselmo-91cc\";\nconst base = \"master\";\nfor (const angulo of [\"lucas\", \"marcos\", \"juan\", \"mateo\"]) {\n  const s = apostolPrompt({ angulo, worktreeDir: wt, baseRef: base });\n  r.ok(`[${angulo}] devuelve un string no vacío`, typeof s === \"string\" && s.length > 0);\n  r.ok(`[${angulo}] incrusta el worktreeDir de Anselmo`, s.includes(wt));\n  r.ok(`[${angulo}] incrusta el baseRef para el diff master-vs-rama`, s.includes(base));\n  r.ok(`[${angulo}] pide verificar que la documentación describe bien las features`, /(verific|comprob|confirm)/i.test(s) && /(document|backbone)/i.test(s) && /features?/i.test(s));\n}"
      },
      {
        "ref": "voces",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "linaReconPrompt convierte la lista de Mateo en tests obligatorios y limita la vista",
        "dado": "linaReconPrompt({testsDir, docsDir, huecosMateo}) con una lista de huecos de Mateo no vacía",
        "cuando": "se genera el prompt",
        "entonces": "el string (a) marca los huecos de Mateo como entrada OBLIGATORIA que se vuelve tests nuevos, y (b) instruye planificar mirando SOLO los tests + el worktree/documentación de Anselmo (testsDir/docsDir), no el código vivo",
        "id": "T-24-05",
        "codigo": "const { linaReconPrompt } = await import(\"../../scripts/lib/forge-prompts.js\");\nconst huecos = [\"no hay test del gate en rojo\", \"falta cubrir el ángulo inválido del apóstol\"];\nconst s = linaReconPrompt({ testsDir: \"/wt/tests\", docsDir: \"/wt/backbone\", huecosMateo: huecos });\nr.ok(\"devuelve un string no vacío\", typeof s === \"string\" && s.length > 0);\n// (a) los huecos de Mateo como entrada OBLIGATORIA que se vuelve tests nuevos\nr.ok(\"marca los huecos de Mateo como OBLIGATORIOS\", /obligator/i.test(s));\nr.ok(\"enumera el primer hueco de Mateo\", s.includes(huecos[0]));\nr.ok(\"enumera el segundo hueco de Mateo\", s.includes(huecos[1]));\nr.ok(\"dice que los huecos se vuelven tests nuevos\", /tests?/i.test(s));\n// (b) planificar mirando SOLO los tests + el worktree/docs de Anselmo\nr.ok(\"referencia testsDir (lo que SÍ puede mirar)\", s.includes(\"/wt/tests\"));\nr.ok(\"referencia docsDir (worktree/documentación de Anselmo)\", s.includes(\"/wt/backbone\"));\nr.ok(\"acota la vista: SOLO tests+docs, no el código vivo\", /\\bsolo\\b/i.test(s) && /(c[oó]digo\\s+vivo|no\\s+(mires|leas|el\\s+c[oó]digo))/i.test(s));"
      },
      {
        "ref": "voces",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "miguelGigantePrompt ordena reconstruir en ultracode desde el código de master",
        "dado": "miguelGigantePrompt({rama, planPath, docsDir, masterRef, reanudacion:false})",
        "cuando": "se genera el prompt",
        "entonces": "el string pide reconstruir con esfuerzo ultracode partiendo del código de masterRef, usando los tests+plan(planPath)+docs(docsDir) como brújula, hasta que TODOS los tests pasen, e incrusta la rama",
        "id": "T-24-06",
        "codigo": "const { miguelGigantePrompt } = await import(\"../../scripts/lib/forge-prompts.js\");\nconst s = miguelGigantePrompt({ rama: \"ciclo/recon-3\", planPath: \"/wt/plan-recon.md\", docsDir: \"/wt/backbone\", masterRef: \"master\", reanudacion: false });\nr.ok(\"devuelve un string no vacío\", typeof s === \"string\" && s.length > 0);\nr.ok(\"pide esfuerzo ultracode\", /ultracode/i.test(s));\nr.ok(\"parte del código de masterRef\", s.includes(\"master\") && /(master|partiendo|desde el c[oó]digo)/i.test(s));\nr.ok(\"usa el plan (planPath) como brújula\", s.includes(\"/wt/plan-recon.md\"));\nr.ok(\"usa los docs (docsDir) como brújula\", s.includes(\"/wt/backbone\"));\nr.ok(\"manda reconstruir hasta que TODOS los tests pasen\", /tests?/i.test(s) && /(todos|pasen|verde)/i.test(s));\nr.ok(\"incrusta la rama\", s.includes(\"ciclo/recon-3\"));"
      },
      {
        "ref": "voces",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "la variante de reanudación de Miguel cambia el texto",
        "dado": "miguelGigantePrompt con reanudacion:true frente a reanudacion:false (mismos demás args)",
        "cuando": "se comparan ambos strings",
        "entonces": "con reanudacion:true el texto incluye la consigna de 'seguir desde donde lo dejó' (continuar en la misma rama, no empezar de cero) y con reanudacion:false no la incluye; los dos strings difieren",
        "id": "T-24-07",
        "codigo": "const { miguelGigantePrompt } = await import(\"../../scripts/lib/forge-prompts.js\");\nconst base = { rama: \"ciclo/recon-3\", planPath: \"/wt/plan-recon.md\", docsDir: \"/wt/backbone\", masterRef: \"master\" };\nconst fresco = miguelGigantePrompt({ ...base, reanudacion: false });\nconst reanuda = miguelGigantePrompt({ ...base, reanudacion: true });\nr.ok(\"ambos son strings no vacíos\", typeof fresco === \"string\" && fresco.length > 0 && typeof reanuda === \"string\" && reanuda.length > 0);\nr.ok(\"los dos strings DIFIEREN\", fresco !== reanuda);\nr.ok(\"reanudacion:true incluye 'seguir desde donde lo dejó'\", /(sigue|contin[uú]a|donde lo dej|donde se qued|reanud)/i.test(reanuda));\nr.ok(\"reanudacion:true insiste en la MISMA rama (no empezar de cero)\", reanuda.includes(base.rama) && /(misma rama|no empieces|no empezar|no desde cero)/i.test(reanuda));\nr.ok(\"reanudacion:false NO incluye la consigna de reanudar\", !/(donde lo dej|donde se qued|reanud)/i.test(fresco));"
      },
      {
        "ref": "voces",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "los prompt-builders son puros y deterministas",
        "dado": "cualquiera de los builders (anselmoDocPrompt, apostolPrompt, linaReconPrompt, miguelGigantePrompt)",
        "cuando": "se llama dos veces con los mismos argumentos",
        "entonces": "devuelve exactamente el mismo string ambas veces y no lee disco, store ni variables de entorno (función pura; solo construye texto)",
        "id": "T-24-08",
        "codigo": "const mod = await import(\"../../scripts/lib/forge-prompts.js\");\n// determinismo: dos llamadas con los mismos args → el MISMO string\nconst casos = [\n  () => mod.anselmoDocPrompt({ worktreeDir: \"/wt/a\", baseRef: \"master\", objetivo: \"el forge\" }),\n  () => mod.apostolPrompt({ angulo: \"mateo\", worktreeDir: \"/wt/a\", baseRef: \"master\" }),\n  () => mod.linaReconPrompt({ testsDir: \"/wt/tests\", docsDir: \"/wt/docs\", huecosMateo: [\"x\"] }),\n  () => mod.miguelGigantePrompt({ rama: \"r\", planPath: \"/p\", docsDir: \"/d\", masterRef: \"master\", reanudacion: false }),\n];\nfor (const f of casos) {\n  const a = f(); const b = f();\n  r.ok(\"misma entrada → mismo string (determinista)\", typeof a === \"string\" && a.length > 0 && a === b);\n}\n// pureza por fuente: el módulo NO lee env ni toca disco (Contrato B)\nconst fs = await import(\"fs\"); const path = await import(\"path\"); const { fileURLToPath } = await import(\"url\");\nconst ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), \"..\", \"..\");\nconst src = fs.readFileSync(path.join(ROOT, \"scripts\", \"lib\", \"forge-prompts.js\"), \"utf8\");\nr.ok(\"forge-prompts.js no lee process.env\", !/process\\.env/.test(src));\nr.ok(\"forge-prompts.js no importa fs (no toca disco)\", !/from\\s+['\\\"]fs['\\\"]/.test(src) && !/require\\(['\\\"]fs['\\\"]\\)/.test(src));"
      },
      {
        "ref": "orquestador",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "abre rama de ciclo, mueve el worktree a ella y guarda baseRef=master",
        "dado": "el orquestador del ciclo de reconstrucción al arrancar (con git mockeado)",
        "cuando": "ejecuta el paso 1",
        "entonces": "crea una rama de ciclo nueva, deja el worktree en esa rama (NO en master) para tener un master-vs-rama comparable, y registra baseRef='master' para los diffs",
        "id": "T-24-09",
        "codigo": "let mod = {};\nfor (const p of [\"../../scripts/lib/forge-recon.js\", \"../../scripts/lib/forge-firme.js\"]) {\n  try { Object.assign(mod, await import(p)); } catch {}\n}\nconst keys = Object.keys(mod);\nr.ok(\"el cerebro del ciclo expone algo (forge-recon.js / forge-firme.js)\", keys.length > 0);\nconst fn = mod.abrirCiclo || mod.planCiclo || mod.nuevaRamaCiclo || mod.crearRamaCiclo\n  || mod[keys.find((k) => typeof mod[k] === \"function\" && /(abrir|nueva|crear|plan).*(rama|ciclo)|ciclo$/i.test(k))];\nr.ok(\"expone un planificador de apertura del ciclo (paso 1)\", typeof fn === \"function\");\nif (typeof fn !== \"function\") return;\nlet res;\ntry { res = fn({ baseBranch: \"master\" }); } catch { try { res = fn(); } catch { res = null; } }\nr.ok(\"devuelve un descriptor del ciclo\", res && typeof res === \"object\");\nif (!res || typeof res !== \"object\") return;\nconst baseRef = res.baseRef ?? res.base ?? res.baseRev;\nconst rama = res.rama ?? res.branch ?? res.ramaCiclo;\nr.eq(\"registra baseRef = master (para diffear master-vs-rama)\", baseRef, \"master\");\nr.ok(\"crea una rama de ciclo nueva, distinta de master (el worktree pasa a ELLA)\", typeof rama === \"string\" && rama.length > 0 && rama !== \"master\");"
      },
      {
        "ref": "orquestador",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "el orquestador es un fence: ejecuta los pasos en orden fijo 1→9",
        "dado": "el driver determinista del ciclo",
        "cuando": "corre el ciclo completo (con todo mockeado y el gate verde)",
        "entonces": "ejecuta en secuencia rama → Anselmo documenta → 4 apóstoles → Lina+Ana Liz plan/tests → gate de oro → limpieza de código → volcado plan+docs → Miguel gigante, sin saltarse ni reordenar pasos",
        "id": "T-24-10",
        "codigo": "let mod = {};\nfor (const p of [\"../../scripts/lib/forge-recon.js\", \"../../scripts/lib/forge-firme.js\"]) {\n  try { Object.assign(mod, await import(p)); } catch {}\n}\nconst keys = Object.keys(mod);\nr.ok(\"el cerebro del ciclo existe\", keys.length > 0);\nconst pasosFn = mod.pasosCiclo || mod.pasos || mod.fence || mod.secuencia\n  || mod[keys.find((k) => typeof mod[k] === \"function\" && /(paso|fence|secuenc|orden)/i.test(k))];\nr.ok(\"expone la secuencia de pasos del fence\", typeof pasosFn === \"function\");\nif (typeof pasosFn !== \"function\") return;\nconst pasos = pasosFn();\nr.ok(\"devuelve la lista de pasos (>=8)\", Array.isArray(pasos) && pasos.length >= 8);\nif (!Array.isArray(pasos)) return;\nconst texto = pasos.map((p) => (typeof p === \"string\" ? p : JSON.stringify(p)).toLowerCase());\nconst idx = (re) => texto.findIndex((t) => re.test(t));\nconst orden = {\n  rama: idx(/rama|ciclo/),\n  anselmo: idx(/anselmo|document/),\n  apostoles: idx(/ap[oó]stol/),\n  lina: idx(/lina|ana\\s*liz|tests?/),\n  gate: idx(/gate|oro/),\n  limpieza: idx(/limpi/),\n  volcado: idx(/volcad/),\n  miguel: idx(/miguel/),\n};\nconst sec = [orden.rama, orden.anselmo, orden.apostoles, orden.lina, orden.gate, orden.limpieza, orden.volcado, orden.miguel];\nr.ok(\"todos los pasos del fence están presentes\", sec.every((i) => i >= 0));\nr.ok(\"orden fijo 1→9: rama→Anselmo→apóstoles→Lina/AnaLiz→gate→limpieza→volcado→Miguel\", sec.every((v, k) => k === 0 || sec[k - 1] < v));"
      },
      {
        "ref": "orquestador",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "lanza los 4 apóstoles EN PARALELO, mismo apóstol, 4 ángulos",
        "dado": "el paso 3 del orquestador con el worktree de Anselmo ya listo",
        "cuando": "verifica la documentación",
        "entonces": "lanza 4 instancias EN PARALELO del mismo apóstol, cada una arrancando de cero con uno de los ángulos (lucas/marcos/juan/mateo) + el worktree de Anselmo + baseRef, y recoge los 4 testimonios",
        "id": "T-24-11",
        "codigo": "let mod = {};\nfor (const p of [\"../../scripts/lib/forge-recon.js\", \"../../scripts/lib/forge-firme.js\"]) {\n  try { Object.assign(mod, await import(p)); } catch {}\n}\nconst keys = Object.keys(mod);\nr.ok(\"el cerebro del ciclo existe\", keys.length > 0);\nconst pasosFn = mod.pasosCiclo || mod.pasos || mod.fence || mod.secuencia\n  || mod[keys.find((k) => typeof mod[k] === \"function\" && /(paso|fence|secuenc|orden)/i.test(k))];\nr.ok(\"expone la secuencia de pasos\", typeof pasosFn === \"function\");\nif (typeof pasosFn !== \"function\") return;\nconst pasos = pasosFn();\nif (!Array.isArray(pasos)) { r.ok(\"pasos es un array\", false); return; }\nconst i = pasos.findIndex((p) => /ap[oó]stol/i.test(typeof p === \"string\" ? p : JSON.stringify(p)));\nr.ok(\"hay un paso de apóstoles\", i >= 0);\nconst pa = pasos[i];\n// fan-out de 4 EN PARALELO, mismo apóstol, 4 ángulos\nconst paralelo = (pa && typeof pa === \"object\" && (pa.paralelo === true || pa.parallel === true || (Array.isArray(pa.apostoles) && pa.apostoles.length === 4) || pa.n === 4))\n  || /paralel/i.test(JSON.stringify(pa));\nr.ok(\"los 4 apóstoles arrancan EN PARALELO (un solo paso fan-out, no 4 en serie)\", paralelo);\nconst json = JSON.stringify(pa).toLowerCase();\nconst angulos = [\"lucas\", \"marcos\", \"juan\", \"mateo\"];\nr.ok(\"el paso nombra los 4 ángulos (lucas/marcos/juan/mateo)\", angulos.every((a) => json.includes(a)) || (pa && pa.n === 4) || (Array.isArray(pa.apostoles) && pa.apostoles.length === 4));"
      },
      {
        "ref": "orquestador",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "gate de oro verde deja continuar el ciclo",
        "dado": "la batería nueva de tests corrida contra el código de MASTER",
        "cuando": "el gate de oro sale verde",
        "entonces": "el ciclo continúa hacia la limpieza, el volcado y el lanzamiento de Miguel (la batería describe lo que ya existía)",
        "id": "T-24-12",
        "codigo": "let mod = {};\nfor (const p of [\"../../scripts/lib/forge-recon.js\", \"../../scripts/lib/forge-firme.js\"]) {\n  try { Object.assign(mod, await import(p)); } catch {}\n}\nconst keys = Object.keys(mod);\nr.ok(\"el cerebro del ciclo existe\", keys.length > 0);\nconst gate = mod.gateDeOro || mod.evaluarGate || mod.gate\n  || mod[keys.find((k) => typeof mod[k] === \"function\" && /gate|oro/i.test(k))];\nr.ok(\"expone el gate de oro\", typeof gate === \"function\");\nif (typeof gate !== \"function\") return;\nconst verde = gate({ pass: 17, fail: 0 });\nconst accV = String(verde && (verde.accion ?? verde.action ?? verde));\nr.ok(\"gate verde (0 fallos) → el ciclo CONTINÚA hacia limpieza/volcado/Miguel\", /continu|seguir|avanz|ok|true/i.test(accV) || verde === true || (verde && verde.ok === true));"
      },
      {
        "ref": "orquestador",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "gate de oro rojo aborta el ciclo y no lanza a Miguel",
        "dado": "la batería nueva corrida contra MASTER",
        "cuando": "el gate de oro sale rojo (algún test falla)",
        "entonces": "el orquestador ABORTA el ciclo: no limpia, no vuelca y NO lanza al Miguel gigante (los tests inventaban comportamiento)",
        "id": "T-24-13",
        "codigo": "let mod = {};\nfor (const p of [\"../../scripts/lib/forge-recon.js\", \"../../scripts/lib/forge-firme.js\"]) {\n  try { Object.assign(mod, await import(p)); } catch {}\n}\nconst keys = Object.keys(mod);\nr.ok(\"el cerebro del ciclo existe\", keys.length > 0);\nconst gate = mod.gateDeOro || mod.evaluarGate || mod.gate\n  || mod[keys.find((k) => typeof mod[k] === \"function\" && /gate|oro/i.test(k))];\nr.ok(\"expone el gate de oro\", typeof gate === \"function\");\nif (typeof gate !== \"function\") return;\nconst rojo = gate({ pass: 15, fail: 2 });\nconst accR = String(rojo && (rojo.accion ?? rojo.action ?? rojo));\nr.ok(\"gate rojo (algún fallo) → el ciclo ABORTA (no limpia, no vuelca, NO lanza a Miguel)\", /abort|parar|detener|stop|fail|false/i.test(accR) || rojo === false || (rojo && rojo.ok === false));\n// y que verde y rojo den veredictos distintos\nconst verde = gate({ pass: 17, fail: 0 });\nr.ok(\"verde y rojo dan veredictos DISTINTOS\", JSON.stringify(verde) !== JSON.stringify(rojo));"
      },
      {
        "ref": "orquestador",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "la limpieza deja en la rama solo los tests",
        "dado": "el paso 6 tras pasar el gate",
        "cuando": "el orquestador limpia la rama",
        "entonces": "borra de la rama todo el código que NO sean los tests, dejando los ficheros de test (más plan/docs) y quitando la implementación",
        "id": "T-24-14",
        "codigo": "let mod = {};\nfor (const p of [\"../../scripts/lib/forge-recon.js\", \"../../scripts/lib/forge-firme.js\"]) {\n  try { Object.assign(mod, await import(p)); } catch {}\n}\nconst keys = Object.keys(mod);\nr.ok(\"el cerebro del ciclo existe\", keys.length > 0);\nconst limpia = mod.planLimpieza || mod.limpiezaRama || mod.limpiarRama\n  || mod[keys.find((k) => typeof mod[k] === \"function\" && /limpi/i.test(k))];\nr.ok(\"expone el plan de limpieza de la rama (paso 6)\", typeof limpia === \"function\");\nif (typeof limpia !== \"function\") return;\nconst cambiados = [\n  \"forge/tests/tarea-24.test.js\",\n  \"scripts/reconstruir.js\",\n  \"scripts/lib/forge-prompts.js\",\n  \"forge/tests/25-recon.test.js\",\n  \"public/forge/index.html\",\n];\nlet res;\ntry { res = limpia(cambiados); } catch { res = limpia({ cambiados }); }\nconst conserva = Array.isArray(res) ? res : (res && (res.conserva || res.keep || res.sobreviven));\nr.ok(\"devuelve qué ficheros se conservan\", Array.isArray(conserva));\nif (!Array.isArray(conserva)) return;\nr.ok(\"SOLO sobreviven ficheros de test\", conserva.every((p) => /\\.test\\.js$/.test(p) || /[\\\\/]tests[\\\\/]/.test(p)));\nr.ok(\"conserva los .test.js que cambiaron\", conserva.includes(\"forge/tests/tarea-24.test.js\") && conserva.includes(\"forge/tests/25-recon.test.js\"));\nr.ok(\"borra (no conserva) el código que no es test\", !conserva.includes(\"scripts/reconstruir.js\") && !conserva.includes(\"public/forge/index.html\"));"
      },
      {
        "ref": "orquestador",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "el volcado lleva plan+docs pero NO los libros de los apóstoles",
        "dado": "el paso 7 (volcar la rama)",
        "cuando": "el orquestador vuelca la rama",
        "entonces": "el resultado incluye el plan de Lina y la documentación de Anselmo, pero EXCLUYE los 4 testimonios/libros de los apóstoles (Lucas/Marcos/Juan/Mateo)",
        "id": "T-24-15",
        "codigo": "let mod = {};\nfor (const p of [\"../../scripts/lib/forge-recon.js\", \"../../scripts/lib/forge-firme.js\"]) {\n  try { Object.assign(mod, await import(p)); } catch {}\n}\nconst keys = Object.keys(mod);\nr.ok(\"el cerebro del ciclo existe\", keys.length > 0);\nconst filtra = mod.filtrarVolcado || mod.volcadoFinal || mod.excluyeLibrosApostoles\n  || mod[keys.find((k) => typeof mod[k] === \"function\" && /(volcad|apostol|libro)/i.test(k))];\nr.ok(\"expone el filtro del volcado (paso 7)\", typeof filtra === \"function\");\nif (typeof filtra !== \"function\") return;\nconst files = [\n  \"forge/backbone/plan-recon.md\",\n  \"forge/backbone/backbone.md\",\n  \"forge/backbone/apostoles/lucas.md\",\n  \"forge/backbone/apostoles/marcos.md\",\n  \"forge/backbone/apostoles/juan.md\",\n  \"forge/backbone/apostoles/mateo.md\",\n];\nlet out;\ntry { out = filtra(files); } catch { out = filtra({ files }); }\nconst kept = Array.isArray(out) ? out : (out && (out.conserva || out.keep || out.volcado));\nr.ok(\"devuelve la lista volcada\", Array.isArray(kept));\nif (!Array.isArray(kept)) return;\nr.ok(\"incluye el plan de Lina\", kept.some((p) => /plan/i.test(p)));\nr.ok(\"incluye los docs de Anselmo (backbone.md)\", kept.some((p) => /backbone\\.md$/i.test(p)));\nr.ok(\"EXCLUYE los 4 libros de los apóstoles (lucas/marcos/juan/mateo)\", kept.every((p) => !/(lucas|marcos|juan|mateo)\\b/i.test(p)));"
      },
      {
        "ref": "orquestador",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "reanudación: si Miguel cae, otro sigue en la misma rama",
        "dado": "el Miguel gigante del paso 8 que se cae a medias",
        "cuando": "el orquestador detecta la caída",
        "entonces": "arranca otro Miguel con reanudacion=true sobre la MISMA rama (continúa desde donde lo dejó), sin empezar la reconstrucción de cero",
        "id": "T-24-16",
        "codigo": "let mod = {};\nfor (const p of [\"../../scripts/lib/forge-recon.js\", \"../../scripts/lib/forge-firme.js\"]) {\n  try { Object.assign(mod, await import(p)); } catch {}\n}\nconst keys = Object.keys(mod);\nr.ok(\"el cerebro del ciclo existe\", keys.length > 0);\nconst decide = mod.decidirReanudacion || mod.reanudarMiguel || mod.trasCaidaMiguel\n  || mod[keys.find((k) => typeof mod[k] === \"function\" && /(reanud|caid|reinten)/i.test(k))];\nr.ok(\"expone la decisión de reanudación de Miguel (paso 9)\", typeof decide === \"function\");\nif (typeof decide !== \"function\") return;\nlet res;\ntry { res = decide({ caido: true, todosVerde: false, rama: \"ciclo/recon-7\" }); } catch { res = null; }\nr.ok(\"ante una caída devuelve una decisión\", res && typeof res === \"object\");\nif (!res || typeof res !== \"object\") return;\nconst acc = String(res.accion ?? res.action ?? \"\");\nr.ok(\"lanza OTRO Miguel (no se rinde)\", res.reanudacion === true || res.relanzar === true || /reanud|relanz|lanzar|otro|continuar/i.test(acc));\nr.ok(\"marca que es reanudación (continúa, no desde cero)\", res.reanudacion === true || /reanud/i.test(JSON.stringify(res)));\nr.eq(\"sigue en la MISMA rama (no crea otra)\", res.rama ?? res.branch, \"ciclo/recon-7\");"
      }
    ],
    "aprobado": false,
    "aprobadoAt": null,
    "generado": true,
    "generadoAt": "2026-06-08T04:36:53.592Z"
  },
  "worktree": "C:\\Users\\TieBlumer\\Documents\\neblla\\.wt\\miguel-t024-1780893785704",
  "branch": "miguel/tarea-024-1780893785704",
  "buildRepo": "C:\\Users\\TieBlumer\\Documents\\neblla",
  "base": "34f0b0bdc33964c778dd0ce8688de74cd834f529",
  "builtAt": "2026-06-08T04:43:06.355Z",
  "construido": true,
  "construidoAt": "2026-06-08T04:50:17.665Z",
  "brought": true,
  "broughtAt": "2026-06-08T05:19:43.627Z",
  "enMaster": true,
  "enMasterAt": "2026-06-08T05:19:44.067Z",
  "masterCommit": "ad9c6af30d2405ce4e33ac5fd8ed585895dcebbc"
}
