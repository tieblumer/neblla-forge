{
  "id": "020",
  "num": 20,
  "title": "Pintar en cada fila de la lista de conversaciones un iconito de estado por cada tarea que nació de ese chat, para ver de un vistazo qué charla parió trabajo y cómo va. Es puro front: cruza las dos listas que el panel ya carga (chats y tareas decoradas) sin endpoint nuevo.",
  "body": "Complejidad: mediana\n\nPartes:\n• iconos-tarea-en-chats — En public/forge/index.html: mantener un índice compartido tareasPorChat (chatId → tareas decoradas) que loadTareas() reconstruye en cada refresco a partir de /api/tareas (cada tarea decorada ya trae fromChat, estado, icon y grupo). loadList() y un re-pintado disparado por loadTareas() leen ese índice y, para cada .chat-item, agrupan las tareas cuyo fromChat === chat.id, las ORDENAN por gravedad usando el orden de GRUPOS (revisar→encurso→porhacer→terminadas) y pintan los iconos (icon de cada tarea) en la fila .chat-item .title, dentro de un contenedor inline; tope de 3 iconos visibles + un '+N' gris cuando sobran, de modo que un ✕ (revisar) nunca quede escondido tras verdes. Sin caché propia: siempre lee la lista viva, así una tarea borrada (que pierde su fromChat vía restoreStubbedMessage) desaparece sola del cruce. Añadir el CSS mínimo para la fila de iconos en .chat-item .title sin romper el truncado del título.  (ficheros: public/forge/index.html)",
  "fromChat": "041",
  "fromMsg": 6,
  "createdAt": "2026-06-07T03:12:38.297Z",
  "plan": {
    "version": 1,
    "resumen": "Pintar en cada fila de la lista de conversaciones un iconito de estado por cada tarea que nació de ese chat, para ver de un vistazo qué charla parió trabajo y cómo va. Es puro front: cruza las dos listas que el panel ya carga (chats y tareas decoradas) sin endpoint nuevo.",
    "complejidad": "mediana",
    "partes": [
      {
        "name": "iconos-tarea-en-chats",
        "hace": "En public/forge/index.html: mantener un índice compartido tareasPorChat (chatId → tareas decoradas) que loadTareas() reconstruye en cada refresco a partir de /api/tareas (cada tarea decorada ya trae fromChat, estado, icon y grupo). loadList() y un re-pintado disparado por loadTareas() leen ese índice y, para cada .chat-item, agrupan las tareas cuyo fromChat === chat.id, las ORDENAN por gravedad usando el orden de GRUPOS (revisar→encurso→porhacer→terminadas) y pintan los iconos (icon de cada tarea) en la fila .chat-item .title, dentro de un contenedor inline; tope de 3 iconos visibles + un '+N' gris cuando sobran, de modo que un ✕ (revisar) nunca quede escondido tras verdes. Sin caché propia: siempre lee la lista viva, así una tarea borrada (que pierde su fromChat vía restoreStubbedMessage) desaparece sola del cruce. Añadir el CSS mínimo para la fila de iconos en .chat-item .title sin romper el truncado del título.",
        "ficheros": [
          "public/forge/index.html"
        ]
      }
    ],
    "contrato": [],
    "aprobado": true,
    "aprobadoAt": "2026-06-07T03:12:49.897Z"
  },
  "paralelizadoAt": "2026-06-07T03:12:38.299Z",
  "threadId": "046",
  "testsPlan": {
    "version": 1,
    "tests": [
      {
        "ref": "iconos-tarea-en-chats",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "Un chat que parió tareas muestra sus iconos de estado en la fila",
        "dado": "La lista trae un chat con id X y /api/tareas devuelve 2 tareas decoradas cuyo fromChat === X, cada una con su icon.",
        "cuando": "Se pinta la lista de conversaciones (loadList + el re-pintado disparado por loadTareas).",
        "entonces": "La fila .chat-item de ese chat contiene, dentro de .title, un contenedor inline con exactamente los iconos de esas 2 tareas (los icon de cada tarea decorada).",
        "id": "T-20-01",
        "codigo": "const fs = await import('fs'); const path = await import('path'); const { fileURLToPath } = await import('url');\nconst ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..');\nconst html = fs.readFileSync(path.join(ROOT, 'public', 'forge', 'index.html'), 'utf8');\nfunction bloque(src, ancla, len = 1600) { const i = src.indexOf(ancla); return i === -1 ? '' : src.slice(i, i + len); }\n// existe el índice compartido chatId -> tareas\nr.ok('[T-20-01] existe el índice tareasPorChat', /tareasPorChat/.test(html));\n// loadTareas() reconstruye el cruce agrupando por fromChat\nconst lt = bloque(html, 'async function loadTareas(', 2200);\nr.ok('[T-20-01] loadTareas alimenta tareasPorChat', /tareasPorChat/.test(lt));\nr.ok('[T-20-01] el cruce se hace por fromChat', /fromChat/.test(lt) || /fromChat/.test(html));\n// se pintan los iconos de cada tarea decorada (su .icon) en la fila .chat-item .title\nconst pintarChats = bloque(html, '.chat-item', 0) ; // anchor sólo para asegurar contexto\nr.ok('[T-20-01] el cruce indexa/lee por id de chat', /tareasPorChat\\s*\\[/.test(html));\nr.ok('[T-20-01] los iconos pintados salen del .icon de la tarea decorada', /\\.icon\\b/.test(html));"
      },
      {
        "ref": "iconos-tarea-en-chats",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "Los iconos se ordenan por gravedad según el orden de GRUPOS",
        "dado": "Un chat con tareas en distintos grupos (p.ej. una en 'terminadas' y otra en 'revisar') que cuelgan del mismo fromChat.",
        "cuando": "Se pintan los iconos de esa fila.",
        "entonces": "El icono de 'revisar' aparece antes que el de 'terminadas', siguiendo el orden revisar→encurso→porhacer→terminadas.",
        "id": "T-20-02",
        "codigo": "const fs = await import('fs'); const path = await import('path'); const { fileURLToPath } = await import('url');\nconst ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..');\nconst html = fs.readFileSync(path.join(ROOT, 'public', 'forge', 'index.html'), 'utf8');\n// los iconos se ordenan por gravedad siguiendo el orden de GRUPOS\n// el orden canónico revisar->encurso->porhacer->terminadas debe aparecer en el front\nr.ok('[T-20-02] el front conoce el orden de grupos (revisar..terminadas)',\n  /revisar[\\s\\S]{0,80}encurso[\\s\\S]{0,80}porhacer[\\s\\S]{0,80}terminadas/.test(html));\n// y se aplica con un sort/ordenación sobre las tareas del chat antes de pintar\nr.ok('[T-20-02] hay una ordenación (sort) de las tareas del chat', /\\.sort\\s*\\(/.test(html));\n// la ordenación se apoya en el grupo de cada tarea, no en otra cosa\nr.ok('[T-20-02] la ordenación mira el grupo de la tarea', /\\bgrupo\\b/.test(html));"
      },
      {
        "ref": "iconos-tarea-en-chats",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "Tope de 3 iconos visibles más un '+N' cuando sobran",
        "dado": "Un chat con 5 tareas colgando de su fromChat.",
        "cuando": "Se pinta su fila.",
        "entonces": "Se muestran como mucho 3 iconos y un indicador '+2' en gris; y como están ordenados por gravedad, un ✕ (revisar) nunca queda escondido tras los verdes.",
        "id": "T-20-03",
        "codigo": "const fs = await import('fs'); const path = await import('path'); const { fileURLToPath } = await import('url');\nconst ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..');\nconst html = fs.readFileSync(path.join(ROOT, 'public', 'forge', 'index.html'), 'utf8');\n// tope de 3 iconos visibles: hay un recorte a 3\nr.ok('[T-20-03] se recortan los iconos a un tope de 3', /slice\\(\\s*0\\s*,\\s*3\\s*\\)/.test(html) || /\\b3\\b/.test(html) && /slice\\(/.test(html));\n// y un indicador '+N' cuando sobran (concatenación con '+')\nr.ok(\"[T-20-03] hay un indicador de exceso con prefijo '+'\", /['\"`]\\+['\"`]\\s*\\+/.test(html) || /\\+['\"`]\\s*\\+/.test(html));\n// el '+N' va en gris (color apagado/muted)\nr.ok('[T-20-03] el contador de exceso usa un color apagado (gris/muted)', /muted/.test(html) || /#?(888|999|aaa|gray|grey)/i.test(html));"
      },
      {
        "ref": "iconos-tarea-en-chats",
        "nivel": "persistente",
        "estado": "pasa",
        "titulo": "Un chat sin tareas no muestra iconos",
        "dado": "Un chat cuyo id no es el fromChat de ninguna tarea de /api/tareas.",
        "cuando": "Se pinta la lista.",
        "entonces": "Su fila .chat-item .title no contiene el contenedor de iconos (o está vacío) y el título se ve normal.",
        "id": "T-20-04",
        "codigo": "const fs = await import('fs'); const path = await import('path'); const { fileURLToPath } = await import('url');\nconst ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..');\nconst html = fs.readFileSync(path.join(ROOT, 'public', 'forge', 'index.html'), 'utf8');\nfunction bloque(src, ancla, len = 1800) { const i = src.indexOf(ancla); return i === -1 ? '' : src.slice(i, i + len); }\n// un chat sin tareas no debe pintar contenedor de iconos: el pintado va guardado por\n// un chequeo de \"hay tareas\" (length) antes de crear/insertar el contenedor.\n// Buscamos la zona donde se cruza por chat (tareasPorChat[...]) y comprobamos que\n// hay un guard de longitud cerca.\nconst idx = html.indexOf('tareasPorChat[');\nconst zona = idx === -1 ? html : html.slice(Math.max(0, idx - 200), idx + 1200);\nr.ok('[T-20-04] el cruce por chat existe', idx !== -1 || /tareasPorChat/.test(html));\nr.ok('[T-20-04] el pintado de iconos está guardado por un chequeo de longitud (sin tareas, sin iconos)',\n  /\\.length/.test(zona) && /(if|\\?|&&|continue|return)/.test(zona));"
      },
      {
        "ref": "iconos-tarea-en-chats",
        "nivel": "temporal",
        "estado": "pasa",
        "titulo": "Sin caché propia: una tarea que pierde su fromChat desaparece del cruce al refrescar",
        "dado": "Un chat que mostraba 2 iconos y un siguiente /api/tareas en el que una de esas tareas ya no tiene ese fromChat (fue borrada/restaurada).",
        "cuando": "loadTareas() vuelve a refrescar y re-pinta.",
        "entonces": "La fila pasa a mostrar 1 icono, leyendo siempre la lista viva sin estado obsoleto.",
        "id": "T-20-05",
        "codigo": "const fs = await import('fs'); const path = await import('path'); const { fileURLToPath } = await import('url');\nconst ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..');\nconst html = fs.readFileSync(path.join(ROOT, 'public', 'forge', 'index.html'), 'utf8');\nfunction bloque(src, ancla, len = 2200) { const i = src.indexOf(ancla); return i === -1 ? '' : src.slice(i, i + len); }\nconst lt = bloque(html, 'async function loadTareas(');\n// sin caché propia: loadTareas RECONSTRUYE el índice en cada refresco (reasignación/reset),\n// no lo va acumulando — así una tarea que pierde su fromChat desaparece sola del cruce.\nr.ok('[T-20-05] loadTareas reconstruye el índice tareasPorChat en cada refresco',\n  /tareasPorChat\\s*=/.test(lt) || /tareasPorChat\\s*=\\s*\\{\\s*\\}/.test(html) || /tareasPorChat\\.clear\\(/.test(lt));\n// y dispara un re-pintado de la lista de chats tras refrescar las tareas\nr.ok('[T-20-05] loadTareas dispara el re-pintado de los iconos en la lista de chats',\n  /loadList\\(/.test(lt) || /pintarIconos|repintar|pintarChat|refreshChats|renderChat/i.test(lt));"
      },
      {
        "ref": "general",
        "nivel": "temporal",
        "estado": "pasa",
        "titulo": "El truncado del título sigue intacto con la fila de iconos",
        "dado": "Un chat con título largo y con iconos de tareas en su fila.",
        "cuando": "Se renderiza la fila .chat-item .title.",
        "entonces": "El título sigue truncándose (no desborda ni empuja la fila) y los iconos conviven en su contenedor inline sin romper el layout.",
        "id": "T-20-06",
        "codigo": "const fs = await import('fs'); const path = await import('path'); const { fileURLToPath } = await import('url');\nconst ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..');\nconst html = fs.readFileSync(path.join(ROOT, 'public', 'forge', 'index.html'), 'utf8');\nfunction bloque(src, ancla, len = 240) { const i = src.indexOf(ancla); return i === -1 ? '' : src.slice(i, i + len); }\n// el truncado del título sigue intacto: .chat-item .title conserva overflow/ellipsis/nowrap\nconst titleCss = bloque(html, '.chat-item .title', 260);\nr.ok('[T-20-06] .chat-item .title sigue con overflow hidden', /overflow:\\s*hidden/.test(titleCss));\nr.ok('[T-20-06] .chat-item .title sigue con text-overflow: ellipsis', /text-overflow:\\s*ellipsis/.test(titleCss));\nr.ok('[T-20-06] .chat-item .title sigue con white-space: nowrap', /white-space:\\s*nowrap/.test(titleCss));\n// y se ha añadido CSS para la fila de iconos como contenedor inline (no en bloque,\n// para convivir con el título truncado sin romper el layout)\nr.ok('[T-20-06] hay CSS para el contenedor inline de iconos en la fila',\n  /inline-flex/.test(html) || /display:\\s*inline/.test(html));"
      }
    ],
    "aprobado": false,
    "aprobadoAt": null,
    "generado": true,
    "generadoAt": "2026-06-07T03:13:23.795Z",
    "ultimaCorrida": "2026-06-07T12:21:44.595Z"
  },
  "worktree": "C:\\Users\\TieBlumer\\Documents\\neblla\\.wt\\miguel-t020-1780832601673",
  "branch": "miguel/tarea-020-1780832601673",
  "buildRepo": "C:\\Users\\TieBlumer\\Documents\\neblla",
  "base": "e48680171df2e3fa73240db6dfdd93e5712d040f",
  "builtAt": "2026-06-07T11:43:23.034Z",
  "construido": true,
  "construidoAt": "2026-06-07T11:45:56.169Z",
  "brought": true,
  "broughtAt": "2026-06-07T12:21:44.864Z"
}
